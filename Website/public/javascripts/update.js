const editProduct = (id) => {
    window.location.href = `http://localhost:3000/${id}/productEdit`
}

const editNewpage = (id) => {
    window.location.href = `http://localhost:3000/${id}/newspaperEdit`
}